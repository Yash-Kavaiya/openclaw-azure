interface:
  display_name: "Openclaw Azure"
  short_description: "Repo-specific patterns and workflows for openclaw-azure"
  default_prompt: "Use the openclaw-azure repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true